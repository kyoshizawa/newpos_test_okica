<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" >

    <data>
        <import type="jp.mcapps.android.multi_payment_terminal.ui.Converters" />
        <variable
            name="viewModel"
            type="jp.mcapps.android.multi_payment_terminal.ui.developer.DeveloperAddDummyDriverViewModel" />
        <variable
            name="handlers"
            type="jp.mcapps.android.multi_payment_terminal.ui.developer.DeveloperEventHandlers" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="@dimen/common_margin"
        android:id="@+id/fragment_developer_add_dummy_trading" >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/common_margin"
            android:orientation="horizontal" >

            <TextView
                android:id="@+id/label_dummy_driver_code"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="18sp"
                android:text="乗務員コード："
                android:textAlignment="viewStart"/>

            <EditText
                android:id="@+id/value_dummy_driver_code"
                android:inputType="number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/stroke_gray"
                android:text="@={viewModel.driverCode}"
                android:layout_gravity="center" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/common_margin"
            android:orientation="horizontal" >

            <TextView
                android:id="@+id/label_dummy_driver_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="18sp"
                android:text="乗務員名："
                android:textAlignment="viewStart"/>

            <EditText
                android:id="@+id/value_dummy_driver_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/stroke_gray"
                android:text="@={viewModel.driverName}"
                android:layout_gravity="center" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/common_margin"
            android:orientation="horizontal" >

            <TextView
                android:id="@+id/label_dummy_created_at"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="18sp"
                android:text="入力日時："
                android:textAlignment="viewStart"/>

            <TextView
                android:id="@+id/value_dummy_created_at"
                android:textColor="@color/black"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/stroke_gray"
                android:textSize="18sp"
                android:hint="入力がない場合は現在日時"
                android:text="@{Converters.dateToString(viewModel.createdAt)}"
                android:onClick="@{view -> handlers.onDatePickerClick(view, viewModel.createdAt, 0)}"
                android:layout_gravity="center" />

        </LinearLayout>

        <Button
            android:id="@+id/btn_add_dummy_driver"
            android:layout_margin="@dimen/common_margin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:layout_gravity="center"
            android:text="ダミー乗務員追加"
            android:onClick="@{view -> handlers.onAddDummyDriverClick(view, viewModel)}"/>
    </LinearLayout>
</layout>